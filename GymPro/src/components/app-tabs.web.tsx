import { Tabs, TabList, TabSlot, TabTrigger, TabTriggerSlotProps } from 'expo-router/ui';
import { Pressable, StyleSheet, Text, View } from 'react-native';
export default function AppTabs() { return <Tabs><TabSlot style={{ height: '100%' }} /><TabList asChild><View style={s.tabs}><TabTrigger name="home" href="/" asChild><Tab label="Today" /></TabTrigger><TabTrigger name="workouts" href={'/workouts' as never} asChild><Tab label="Workouts" /></TabTrigger><TabTrigger name="progress" href="/explore" asChild><Tab label="Progress" /></TabTrigger><TabTrigger name="more" href={'/more' as never} asChild><Tab label="More" /></TabTrigger></View></TabList></Tabs>; }
function Tab({ label, isFocused, ...props }: TabTriggerSlotProps & { label: string }) { return <Pressable {...props} style={s.tab}><Text style={[s.label, isFocused && s.active]}>{label}</Text></Pressable>; }
const s = StyleSheet.create({ tabs: { position: 'absolute', bottom: 16, alignSelf: 'center', padding: 5, flexDirection: 'row', backgroundColor: '#1D3C2A', borderRadius: 18 }, tab: { paddingVertical: 10, paddingHorizontal: 13 }, label: { color: '#A9BBAE', fontSize: 12, fontWeight: '800' }, active: { color: '#D4ED55' } });
