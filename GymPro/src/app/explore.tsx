import { SafeAreaView, ScrollView, StyleSheet, Text, View } from 'react-native';
import { Card, ScreenTitle } from '@/components/ui';
import { useGym } from '@/context/workout-context';
export default function Progress() { const { workouts, measurements } = useGym(); const data = measurements.slice(0, 6).reverse(); const volume = workouts.map((workout) => workout.exercises.reduce((sum, exercise) => sum + exercise.sets * exercise.reps * exercise.weight, 0)); return <SafeAreaView style={s.safe}><ScrollView contentContainerStyle={s.content}><ScreenTitle eyebrow="YOUR TRAINING" title="Progress" /><Card style={s.streak}><Text style={s.big}>8</Text><View><Text style={s.whiteTitle}>Day streak</Text><Text style={s.whiteText}>Keep moving to reach your weekly goal.</Text></View></Card><Text style={s.heading}>Workout frequency</Text><Card><View style={s.bars}>{[45, 78, 58, 92, 67, 84, 100].map((height, index) => <View key={index} style={[s.bar, { height: `${height}%` }, index === 6 && s.lastBar]} />)}</View><Text style={s.caption}>{workouts.length} workouts recorded · this month</Text></Card><Text style={s.heading}>Weight history</Text><Card>{data.length ? data.map((item) => <View key={item.id} style={s.weightRow}><Text style={s.date}>{item.date}</Text><Text style={s.weight}>{item.weight} kg</Text></View>) : <Text style={s.empty}>Add body measurements to create your weight history.</Text>}</Card><Text style={s.heading}>Volume lifted</Text><Card><Text style={s.volume}>{(volume.reduce((sum, value) => sum + value, 0) / 1000).toFixed(1)}k kg</Text><Text style={s.caption}>Total recorded training volume</Text></Card></ScrollView></SafeAreaView>; }
const s = StyleSheet.create({ safe: { flex: 1, backgroundColor: '#F6F7F3' }, content: { padding: 20, paddingBottom: 120, gap: 13 }, streak: { backgroundColor: '#203F2D', borderColor: '#203F2D', flexDirection: 'row', alignItems: 'center', gap: 19 }, big: { color: '#D4ED55', fontSize: 43, fontWeight: '900' }, whiteTitle: { color: '#FFF', fontSize: 16, fontWeight: '800' }, whiteText: { color: '#BED0C3', fontSize: 11, marginTop: 4 }, heading: { color: '#18241E', fontWeight: '800', fontSize: 18, marginTop: 9 }, bars: { height: 105, flexDirection: 'row', alignItems: 'flex-end', justifyContent: 'space-between', borderBottomWidth: 1, borderColor: '#E8ECE6' }, bar: { width: 22, borderRadius: 6, backgroundColor: '#DCE7D5' }, lastBar: { backgroundColor: '#4F8745' }, caption: { color: '#808A82', fontSize: 11, marginTop: 10 }, volume: { color: '#234C2B', fontSize: 27, fontWeight: '900' }, weightRow: { flexDirection: 'row', justifyContent: 'space-between', paddingVertical: 8, borderBottomWidth: 1, borderColor: '#EEF0ED' }, date: { color: '#768078', fontSize: 12 }, weight: { color: '#294F30', fontWeight: '800', fontSize: 12 }, empty: { color: '#7B857D', fontSize: 12, lineHeight: 18 } });
