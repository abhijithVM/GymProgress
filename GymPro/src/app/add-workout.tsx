import { router } from 'expo-router';
import { useState } from 'react';
import { SafeAreaView, ScrollView, StyleSheet, Text, View } from 'react-native';
import { Field, PrimaryButton, ScreenTitle } from '@/components/ui';
import { useGym } from '@/context/workout-context';
export default function AddWorkout() { const { addWorkout } = useGym(); const [name, setName] = useState(''); const [exercise, setExercise] = useState(''); const [sets, setSets] = useState('3'); const [reps, setReps] = useState('10'); const [weight, setWeight] = useState(''); const save = () => { if (!name || !exercise) return; addWorkout({ name, date: new Date().toISOString().slice(0, 10), notes: '', exercises: [{ id: String(Date.now()), name: exercise, sets: Number(sets) || 0, reps: Number(reps) || 0, weight: Number(weight) || 0, completed: true }] }); router.replace('/workouts' as never); }; return <SafeAreaView style={s.safe}><ScrollView contentContainerStyle={s.content}><ScreenTitle eyebrow="NEW ENTRY" title="Add workout" /><Text style={s.hint}>Start simple: one exercise is enough. You can expand this form later.</Text><View style={s.fields}><Field label="WORKOUT NAME" value={name} onChangeText={setName} placeholder="Chest + Triceps" /><Field label="EXERCISE" value={exercise} onChangeText={setExercise} placeholder="Bench press" /><View style={s.triplet}><View style={s.flex}><Field label="SETS" value={sets} onChangeText={setSets} keyboardType="numeric" /></View><View style={s.flex}><Field label="REPS" value={reps} onChangeText={setReps} keyboardType="numeric" /></View><View style={s.flex}><Field label="KG" value={weight} onChangeText={setWeight} keyboardType="numeric" /></View></View></View><PrimaryButton label="Save workout" onPress={save} /></ScrollView></SafeAreaView>; }
const s = StyleSheet.create({ safe: { flex: 1, backgroundColor: '#F6F7F3' }, content: { padding: 20, paddingBottom: 100, gap: 22 }, hint: { color: '#78827A', fontSize: 13, lineHeight: 19 }, fields: { gap: 16 }, triplet: { flexDirection: 'row', gap: 9 }, flex: { flex: 1 } });
