import { router } from 'expo-router';
import { SafeAreaView, ScrollView, StyleSheet, Text, View } from 'react-native';
import { Card, PrimaryButton, ScreenTitle } from '@/components/ui';
import { useGym } from '@/context/workout-context';
export default function Workouts() { const { workouts, toggleExercise } = useGym(); return <SafeAreaView style={s.safe}><ScrollView contentContainerStyle={s.content}><ScreenTitle eyebrow="TRAINING LOG" title="Workouts" /><PrimaryButton label="+ Add workout" onPress={() => router.push('/add-workout' as never)} />{workouts.map((workout) => <Card key={workout.id}><View style={s.row}><View><Text style={s.title}>{workout.name}</Text><Text style={s.date}>{workout.date}</Text></View><Text style={s.count}>{workout.exercises.length} exercises</Text></View><View style={s.exercises}>{workout.exercises.map((exercise) => <Text onPress={() => toggleExercise(workout.id, exercise.id)} key={exercise.id} style={[s.exercise, exercise.completed && s.done]}>{exercise.completed ? '✓' : '○'}  {exercise.name} · {exercise.sets} × {exercise.reps} · {exercise.weight} kg</Text>)}</View></Card>)}</ScrollView></SafeAreaView>; }
const s = StyleSheet.create({ safe: { flex: 1, backgroundColor: '#F6F7F3' }, content: { padding: 20, paddingBottom: 120, gap: 12 }, row: { flexDirection: 'row', justifyContent: 'space-between' }, title: { color: '#1B2C20', fontSize: 16, fontWeight: '800' }, date: { color: '#819087', fontSize: 11, marginTop: 4 }, count: { color: '#62805E', fontSize: 10, fontWeight: '800' }, exercises: { borderTopWidth: 1, borderColor: '#EDF0EB', paddingTop: 12, gap: 10, marginTop: 14 }, exercise: { color: '#6A766D', fontSize: 12 }, done: { color: '#397642', fontWeight: '700' } });
